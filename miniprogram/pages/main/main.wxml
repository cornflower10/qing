    <scroll-view scroll-y bindscrolltolower="lower">
    
    <view class='item_row'>
        <view wx:for="{{list}}" class='item' bindtap='clickItem' data-url='{{item.url}}' wx:key=''>
           
          <image class='item_img' src='{{item.url}}' mode="aspectFill"></image>

        </view>
      </view>
      </scroll-view>
